#include <stdio.h>

int main() {
    int i = 1;

    // Basic while loop that prints numbers 1 through 5
    while (i <= 5) {
        printf("Iteration %d\n", i);
        i++;  // Increment i after each iteration
    }

    return 0;
}
